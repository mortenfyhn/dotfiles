#!/usr/bin/env python3

import sys

def main():
    try:
        f = float(sys.argv[1])
        c = (f - 32.0) * (5/9)
        print(f'{c:.1f} °C')
    except:
        print('usage: fah <temp in °F>')

if __name__ == '__main__':
   main()
