#!/usr/bin/env python3

import sys

def main():
    try:
        f = float(sys.argv[1])
        c = (f - 32.0) * (5/9)
        print('{:.1f} °C'.format(c))
    except:
        print('usage: fah <temp in °F>')

if __name__ == '__main__':
   main()
